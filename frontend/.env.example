# Base URL of the FastAPI backend (src/api.py). Falls back to
# http://localhost:8000 in App.jsx if unset, so this is only needed to
# point at a deployed backend (e.g. in Vercel's project env var settings).
VITE_API_URL=https://your-render-service.onrender.com
