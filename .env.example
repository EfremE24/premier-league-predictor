# Free API key from https://www.football-data.org/client/register
# Used by src/fixtures.py to fetch upcoming Premier League fixtures.
# Local dev: copy this file to .env and fill in your key.
# Production (Render): set as a dashboard env var, never committed.
FOOTBALL_DATA_API_KEY=your-key-here
